import React, { Component, PropTypes } from 'react';

class People extends Component {
  render() {
    return (
      <div>
        this is people component -index.js
      </div>
    );
  }
}

People.propTypes = {
  //children: PropTypes.element
};

export default People;
